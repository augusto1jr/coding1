# Comandos para realizar commit pelo terminal
```
git add / git add arquivo.extensao
git commit -m "commit message"
git push origin main (primeiro push)
git push
git push -f origin main (em caso de problemas no push)
```
